<?xml version="1.0" encoding="UTF-8"?>
<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:validateButton title="${%Test Connection}" progress="${%Testing...}" method="testConnection" with="name" />

    <f:section title="${%Bankdata configurator}">
        <f:entry field="label" title="${%Label}">
            <f:textbox/>
        </f:entry>
    </f:section>
</j:jelly>
